# ===============================
# = Hibernate datasource
# ===============================
spring:
  datasource:
    url: jdbc:mysql://localhost:3308/karetis_project?useUnicode=true
      &useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&
      serverTimezone=UTC&allowLoadLocalInfile=true
    username: root
    password: 1234

  # ===============================
  # = JPA configurations
  # ===============================
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    database-platform: MYSQL
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
        jdbc:
          batch_size: 10000





